<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black">

    <FrameLayout
        android:id="@+id/map3d_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <LinearLayout
        android:id="@+id/pedal_container"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="24dp"
        android:gravity="center_horizontal"
        android:orientation="vertical"
        android:padding="4dp"
        android:background="@drawable/pedal_background">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/accelerateButton"
            style="@style/Widget.MaterialComponents.Button"
            android:layout_width="72dp"
            android:layout_height="72dp"
            android:layout_marginBottom="12dp"
            android:contentDescription="@string/content_description_accelerate"
            android:text="@string/label_accelerate"
            android:textAllCaps="false"
            android:textColor="@color/brand_white"
            app:backgroundTint="@color/brand_primary"
            app:cornerRadius="36dp" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/brakeButton"
            style="@style/Widget.MaterialComponents.Button"
            android:layout_width="72dp"
            android:layout_height="72dp"
            android:contentDescription="@string/content_description_brake"
            android:text="@string/label_brake"
            android:textAllCaps="false"
            android:textColor="@color/brand_white"
            app:backgroundTint="@color/brand_dark_gray"
            app:cornerRadius="36dp" />

    </LinearLayout>

</FrameLayout>
